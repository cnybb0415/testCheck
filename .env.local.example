# Neon Postgres 연결 문자열
# Vercel 대시보드 > Storage > Create Database > Neon 으로 생성한 뒤
# Connect 화면에서 pooled connection string을 복사해 넣으세요.
# (Vercel 프로젝트에 연결해두면 vercel env pull .env.local 로도 받을 수 있습니다.)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
